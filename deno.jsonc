{
  "tasks": {
    "build": "deno run -A build.ts",
    "build:watch": "deno run --cached-only --watch -A build.ts dev",
    "serve": "deno run --watch -A server.ts",
    "dev": "deno task build && deno task serve & deno task build:watch "
  }
}
